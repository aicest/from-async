runs:
  using: 'composite'
  steps:
    - uses: pnpm/action-setup@v3
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: pnpm
    - run: pnpm install
      shell: bash
      env:
        NODE_AUTH_TOKEN: ${{ env.GITHUB_TOKEN }}
